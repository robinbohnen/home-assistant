# Onderhoudsweergave: wat is er stuk of bijna leeg.
#
# De integratie per entiteit staat niet in `states` maar in het entiteitenregister
# (`hass.entities[...].platform`). Is dat register er niet, dan valt de groepering
# terug op het domein van de entiteit.
#
# Alle drie de kaarten lopen bij elke render door álle entiteiten heen. Hier stond
# `update_timer` om dat te begrenzen, maar die optie bestáát niet in button-card
# (het is `triggers_update`) en werd dus stil genegeerd. In plaats daarvan hangen
# de kaarten aan `sensor.time`: die tikt elke minuut, dus precies één render per
# minuut. Dat is meteen ook wat de leeftijden ("9 uur") live houdt op een paneel
# dat dagen open staat — button-card rendert alleen opnieuw als zijn entity
# verandert, en zonder entity bevriezen die teksten.
#
# Leeftijden worden uit last_changed/last_updated berekend, en na een herstart
# staat álles op het herstartmoment. `sensor.home_assistant_gestart` (zie
# packages/9 - Other/Dashboard Context.yaml) laat de kaarten dat verschil
# benoemen in plaats van een misleidend absoluut getal te tonen.
title: Systeem
path: systeem
icon: mdi:heart-pulse
panel: true
background: "#0b0d12"
cards:
  - type: vertical-stack
    card_mod:
      style: |
        :host { height: 100% !important; }
        #root {
          --ov-panel-bg: rgba(22,24,31,0.72);
          --ov-panel-border: 1px solid rgba(255,255,255,0.06);
          gap: 16px !important;
          min-height: calc(100vh - 8px) !important;
          margin: 0 !important;
          padding: 20px 24px 24px 24px !important;
          background: #0b0d12 !important;
          box-sizing: border-box !important;
        }
    cards:
      - type: horizontal-stack
        card_mod:
          style: |
            #root {
              display: grid !important;
              grid-template-columns: 1fr 1fr 1fr !important;
              gap: 16px !important;
              align-items: start !important;
            }
            @media (max-width: 1500px) {
              #root { grid-template-columns: 1fr 1fr !important; }
            }
            @media (max-width: 1000px) {
              #root { grid-template-columns: 1fr !important; }
            }
        cards:
          # =============== ONBESCHIKBARE ENTITEITEN ===============
          # Niet elke 'unavailable' is een storing, en zolang ze op één hoop
          # gaan staat de teller permanent op honderden en zegt hij niets meer.
          # Daarom vier bakken, waarvan alleen de eerste in de badge telt:
          #  - storing    : hier moet iemand iets aan doen
          #  - wees       : `restored: true`, oftewel entiteiten waarvan de
          #                 integratie weg is. Komen nooit meer terug; dit is
          #                 opruimwerk in het entiteitenregister, geen defect.
          #  - afgeleid   : sensoren die een ándere entiteit spiegelen (powercalc
          #                 volgt zijn bron). Ze vallen om als hun bron omvalt —
          #                 dezelfde storing, dubbel geteld.
          #  - ruis       : integraties die per netwerkclient entiteiten maken
          #                 (unifi). Elke gast die ooit op de wifi zat staat er
          #                 nog; 'weg' is daar de normale toestand.
          # De drie laatste bakken verdwijnen niet, ze staan onderaan als telling.
          - type: custom:button-card
            entity: sensor.time
            triggers_update: sensor.time
            show_icon: false
            show_name: false
            show_state: false
            tap_action:
              action: none
            variables:
              # 'unknown' erbij? Vaak ruis van entiteiten die nog nooit een waarde
              # hadden, daarom standaard uit.
              ook_unknown: false
              afgeleid:
                - powercalc
              ruis:
                - unifi
                - unifiprotect
              max_per_groep: 25
              # Staat iets er al even lang weg als Home Assistant draait, dan is
              # het bij de herstart niet teruggekomen. Entiteiten vallen niet
              # allemaal op dezelfde seconde om, vandaar de marge.
              herstart_marge_min: 3
            styles:
              card:
                - background: var(--ov-panel-bg)
                - border: var(--ov-panel-border)
                - border-radius: 20px
                - box-shadow: none
                - padding: 16px 18px
              grid:
                - grid-template-areas: '"t" "lijst"'
                - row-gap: 12px
              custom_fields:
                t:
                  - justify-self: stretch
                lijst:
                  - justify-self: stretch
            custom_fields:
              t: |
                [[[
                  var mee = function(s){ return s.state === 'unavailable' || (variables.ook_unknown && s.state === 'unknown'); };
                  var reg = (hass && hass.entities) ? hass.entities : null;
                  var afgeleid = variables.afgeleid || [];
                  var ruis = variables.ruis || [];
                  var alles = 0;
                  // Zelfde indeling als de lijst hieronder; de badge telt alleen
                  // wat overblijft als storing.
                  var n = Object.keys(states).filter(function(id){
                    var s = states[id];
                    if(!s || !mee(s)) return false;
                    var e = reg ? reg[id] : null;
                    if(e && (e.hidden || e.hidden_by)) return false;
                    alles++;
                    if(s.attributes && s.attributes.restored) return false;
                    var integratie = (e && e.platform) ? e.platform : id.split('.')[0];
                    return afgeleid.indexOf(integratie) === -1 && ruis.indexOf(integratie) === -1;
                  }).length;
                  var kleur = n === 0 ? '#4ade80' : (n > 10 ? '#ef4444' : '#f59e0b');
                  return `<span style="display:flex;align-items:baseline;gap:12px;width:100%">
                    <span style="font-size:12px;font-weight:700;letter-spacing:1.2px;color:rgba(255,255,255,0.55)">STORINGEN</span>
                    <span style="font-size:11.5px;font-weight:600;padding:3px 11px;border-radius:20px;color:${kleur};background:rgba(255,255,255,0.06)">${n}</span>
                    <span style="margin-left:auto;font-size:11.5px;color:rgba(255,255,255,0.4)">${alles} onbeschikbaar totaal</span></span>`;
                ]]]
              lijst: |
                [[[
                  var nu = Date.now();
                  var reg = (hass && hass.entities) ? hass.entities : null;
                  var apparaten = (hass && hass.devices) ? hass.devices : null;
                  var gebieden = (hass && hass.areas) ? hass.areas : null;
                  var mee = function(s){ return s.state === 'unavailable' || (variables.ook_unknown && s.state === 'unknown'); };
                  var afgeleid = variables.afgeleid || [];
                  var ruis = variables.ruis || [];
                  // Hoe lang draait HA al? Een leeftijd die daarmee samenvalt
                  // betekent "niet teruggekomen na de herstart", en dat is iets
                  // anders dan een apparaat dat vanochtend spontaan uitviel.
                  var startS = states['sensor.home_assistant_gestart'];
                  var startMs = startS ? new Date(startS.state).getTime() : NaN;
                  var uptime = isNaN(startMs) ? null : nu - startMs;
                  var marge = (variables.herstart_marge_min || 3) * 60000;
                  // Per bak, per integratie, een map van apparaat -> aantal
                  // entiteiten. Eén apparaat levert vaak 2 tot 4 entiteiten; die
                  // als losse regels tonen maakt de lijst onleesbaar.
                  var bakken = {storing: {}, wees: {}, afgeleid: {}, ruis: {}};
                  Object.keys(states).forEach(function(id){
                    var s = states[id];
                    if(!s || !mee(s)) return;
                    var e = reg ? reg[id] : null;
                    if(e && (e.hidden || e.hidden_by)) return;
                    var integratie = (e && e.platform) ? e.platform : id.split('.')[0];
                    var soort = (s.attributes && s.attributes.restored) ? 'wees'
                              : (afgeleid.indexOf(integratie) !== -1 ? 'afgeleid'
                              : (ruis.indexOf(integratie) !== -1 ? 'ruis' : 'storing'));
                    var devId = (e && e.device_id) ? e.device_id : null;
                    var dev = (devId && apparaten) ? apparaten[devId] : null;
                    var sleutel = devId || ('los:' + id);
                    var gebiedId = (e && e.area_id) || (dev && dev.area_id) || null;
                    var gebied = (gebiedId && gebieden && gebieden[gebiedId]) ? gebieden[gebiedId].name : null;
                    var naam = dev ? (dev.name_by_user || dev.name || devId)
                                   : ((s.attributes && s.attributes.friendly_name) || id);
                    var sinds = s.last_changed ? new Date(s.last_changed).getTime() : null;
                    var duur = sinds === null ? null : nu - sinds;
                    var bak = bakken[soort];
                    if(!bak[integratie]) bak[integratie] = {};
                    var g = bak[integratie];
                    if(!g[sleutel]) g[sleutel] = {naam: naam, gebied: gebied, aantal: 0, duur: duur, los: !devId, dev_id: devId, ent: id};
                    g[sleutel].aantal++;
                    // langst weg binnen het apparaat telt
                    if(duur !== null && (g[sleutel].duur === null || duur > g[sleutel].duur)) g[sleutel].duur = duur;
                  });
                  // Per bak: apparaten gesorteerd op langst weg, plus tellingen
                  // per integratie voor de samenvattingsregels onderaan.
                  var telVan = function(bak){
                    var per = Object.keys(bak).map(function(g){
                      var l = Object.keys(bak[g]).map(function(k){ return bak[g][k]; })
                        .sort(function(a,b){ return (b.duur||0) - (a.duur||0); });
                      return {naam: g, apparaten: l, entiteiten: l.reduce(function(a,x){ return a + x.aantal; }, 0)};
                    });
                    per.sort(function(a,b){ return b.entiteiten - a.entiteiten; });
                    return {per: per,
                            apparaten: per.reduce(function(a,x){ return a + x.apparaten.length; }, 0),
                            entiteiten: per.reduce(function(a,x){ return a + x.entiteiten; }, 0)};
                  };
                  var storing = telVan(bakken.storing);
                  var duurTxt = function(ms){
                    if(ms === null) return 'onbekend';
                    if(uptime !== null && Math.abs(ms - uptime) < marge) return 'sinds herstart';
                    var m = Math.floor(ms / 60000);
                    if(m < 1) return 'net';
                    if(m < 60) return m + ' min';
                    var u = Math.floor(m / 60);
                    if(u < 48) return u + ' uur';
                    var d = Math.floor(u / 24);
                    return d + ' ' + (d === 1 ? 'dag' : 'dagen');
                  };
                  var duurKleur = function(ms){
                    if(ms === null) return 'rgba(255,255,255,0.4)';
                    if(ms > 7 * 86400000) return '#ef4444';
                    if(ms > 86400000) return '#f59e0b';
                    return 'rgba(255,255,255,0.6)';
                  };
                  // Rij klikbaar maken; zie de toelichting bij de Matter-kaart.
                  var klik = function(devId, entId, inner){
                    var doel = devId ? ('/config/devices/device/' + devId) : null;
                    var actie = doel
                      ? `history.pushState(null,'','${doel}');window.dispatchEvent(new CustomEvent('location-changed'));`
                      : `this.dispatchEvent(new CustomEvent('hass-more-info',{detail:{entityId:'${entId}'},bubbles:true,composed:true}));`;
                    return `<a href="${doel || ('/history?entity_id=' + entId)}"
                      style="display:block;color:inherit;text-decoration:none;cursor:pointer;pointer-events:auto"
                      onpointerdown="event.stopPropagation()"
                      onpointerup="event.stopPropagation();${actie}"
                      onclick="event.preventDefault();event.stopPropagation()">${inner}</a>`;
                  };
                  var limiet = variables.max_per_groep || 25;
                  // Storingen: langst weg bovenaan, want dat is meestal het
                  // apparaat dat echt aandacht nodig heeft.
                  var blokken = storing.per.slice()
                    .sort(function(a,b){ return (b.apparaten[0].duur||0) - (a.apparaten[0].duur||0); })
                    .map(function(grp){
                    var app = grp.apparaten;
                    var teveel = Math.max(0, app.length - limiet);
                    var rijen = app.slice(0, limiet).map(function(x){
                      var plek = x.gebied ? `<span style="color:rgba(255,255,255,0.35)"> · ${x.gebied}</span>` : '';
                      var aantal = x.aantal > 1
                        ? `<span style="font-size:11.5px;color:rgba(255,255,255,0.45);white-space:nowrap">${x.aantal} entiteiten</span>` : '';
                      return klik(x.dev_id, x.ent, `<div style="display:flex;align-items:baseline;gap:10px;padding:5px 0;border-top:1px solid rgba(255,255,255,0.04)">
                        <span style="flex:1;text-align:left;font-size:13.5px;color:rgba(255,255,255,0.85);white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${x.naam}${plek}</span>
                        ${aantal}
                        <span style="font-size:13px;font-weight:600;white-space:nowrap;color:${duurKleur(x.duur)}">${duurTxt(x.duur)}</span></div>`);
                    }).join('');
                    return `<div style="margin-bottom:12px">
                      <div style="display:flex;align-items:baseline;gap:8px;padding-bottom:2px;text-align:left">
                        <span style="font-size:12.5px;font-weight:700;color:#38bdf8">${grp.naam}</span>
                        <span style="font-size:11.5px;color:rgba(255,255,255,0.4)">${app.every(function(x){ return x.los; })
                          ? `${grp.entiteiten} entiteiten`
                          : `${app.length} ${app.length === 1 ? 'apparaat' : 'apparaten'} · ${grp.entiteiten} entiteiten`}</span></div>
                      ${rijen}
                      ${teveel ? `<div style="padding:5px 0;font-size:12px;color:rgba(255,255,255,0.45);text-align:left">+ ${teveel} ${teveel === 1 ? 'apparaat' : 'apparaten'} meer</div>` : ''}</div>`;
                  }).join('');
                  if(!storing.entiteiten) blokken = `<div style="font-size:14px;color:#4ade80;text-align:left">Geen storingen.</div>`;
                  // De rest verdwijnt niet uit beeld, maar telt niet mee: één
                  // regel per bak met de uitsplitsing per integratie erachter.
                  var voet = function(kop, uitleg, t){
                    if(!t.entiteiten) return '';
                    var stukjes = t.per.map(function(p){
                      return `<span style="white-space:nowrap"><span style="color:rgba(255,255,255,0.7)">${p.naam}</span> <span style="color:rgba(255,255,255,0.45)">${p.entiteiten}</span></span>`;
                    }).join('<span style="color:rgba(255,255,255,0.2)"> · </span>');
                    return `<div style="border-top:1px solid rgba(255,255,255,0.08);padding-top:10px;margin-top:8px">
                      <div style="display:flex;align-items:baseline;gap:8px;text-align:left">
                        <span style="font-size:12.5px;font-weight:700;color:rgba(255,255,255,0.55)">${kop}</span>
                        <span style="font-size:11.5px;color:rgba(255,255,255,0.4)">${t.entiteiten} entiteiten</span></div>
                      <div style="font-size:11.5px;color:rgba(255,255,255,0.3);text-align:left;padding:1px 0 3px">${uitleg}</div>
                      <div style="font-size:12.5px;line-height:1.7;text-align:left">${stukjes}</div></div>`;
                  };
                  var overig = voet('weesentiteiten', 'integratie bestaat niet meer — op te ruimen in het entiteitenregister', telVan(bakken.wees))
                    + voet('afgeleide sensoren', 'spiegelen een andere entiteit; vallen om als hun bron omvalt', telVan(bakken.afgeleid))
                    + voet('komen en gaan', 'clients die niet altijd aanwezig zijn — weg is hier normaal', telVan(bakken.ruis));
                  var waarschuwing = reg ? '' : `<div style="font-size:11.5px;color:#f59e0b;padding-bottom:8px">Entiteitenregister niet beschikbaar — gegroepeerd op domein in plaats van integratie.</div>`;
                  return `<div style="max-height:calc(100vh - 190px);overflow-y:auto">${waarschuwing}${blokken}${overig}</div>`;
                ]]]

          # =============== BIJNA LEGE BATTERIJEN ===============
          # Deze kaart gaat over batterijen die je zelf vervangt. Omvormers en
          # thuisaccu's melden hun laadtoestand óók als `device_class: battery`,
          # dus die staan elke nacht op een paar procent en maken de kaart dan
          # onterecht rood. Zelfde valkuil als "veelgebruikers" op het Overzicht:
          # de device_class zegt niet wat je denkt dat hij zegt.
          - type: custom:button-card
            entity: sensor.time
            triggers_update: sensor.time
            show_icon: false
            show_name: false
            show_state: false
            tap_action:
              action: none
            variables:
              drempel: 25
              # Integraties die alleen laadtoestand van accu's leveren.
              negeer_integraties:
                - sems
                - goodwe
                - anker_solix
                - marstek
              # Vangnet voor accu's die via een andere integratie binnenkomen
              # (denk aan een omvormer achter MQTT).
              negeer_namen: "omvormer|inverter|thuisbatterij|accu|zonneplan"
            styles:
              card:
                - background: var(--ov-panel-bg)
                - border: var(--ov-panel-border)
                - border-radius: 20px
                - box-shadow: none
                - padding: 16px 18px
              grid:
                - grid-template-areas: '"t" "lijst"'
                - row-gap: 12px
              custom_fields:
                t:
                  - justify-self: stretch
                lijst:
                  - justify-self: stretch
            custom_fields:
              t: |
                [[[
                  var d = variables.drempel;
                  var reg = (hass && hass.entities) ? hass.entities : null;
                  var negeerInt = variables.negeer_integraties || [];
                  var negeerNaam = variables.negeer_namen ? new RegExp(variables.negeer_namen, 'i') : null;
                  var n = Object.keys(states).filter(function(id){
                    var s = states[id];
                    if(!s || !s.attributes || s.attributes.device_class !== 'battery') return false;
                    // Alleen sensoren MÉTEN een batterij. Een number of select met
                    // device_class battery is een instelling: de Megane heeft
                    // `minimum_charge_level` op 20 en `target_charge_level` op 100
                    // staan, en die eerste kwam hier binnen als "bijna lege
                    // batterij" terwijl de auto gewoon op 99% stond.
                    if(id.lastIndexOf('sensor.', 0) !== 0 && id.lastIndexOf('binary_sensor.', 0) !== 0) return false;
                    var e = reg ? reg[id] : null;
                    if(e && (e.hidden || e.hidden_by)) return false;
                    if(negeerInt.indexOf((e && e.platform) ? e.platform : '') !== -1) return false;
                    if(negeerNaam && negeerNaam.test(s.attributes.friendly_name || id)) return false;
                    // Een batterijapparaat op `unavailable` telt mee: die kun je
                    // niet met een schakelaar hebben uitgezet, dus stil betekent
                    // lege batterij of stuk. De pil in de kopbalk noemt precies
                    // dit aantal ("N apparaten stil") en linkt hierheen; stonden
                    // ze hier niet, dan kwam je op een lege lijst uit.
                    if(s.state === 'unavailable') return true;
                    if(id.lastIndexOf('binary_sensor.', 0) === 0) return s.state === 'on';
                    var v = Number(s.state);
                    return !isNaN(v) && v <= d;
                  }).length;
                  var kleur = n === 0 ? '#4ade80' : (n > 5 ? '#ef4444' : '#f59e0b');
                  return `<span style="display:flex;align-items:baseline;gap:12px;width:100%">
                    <span style="font-size:12px;font-weight:700;letter-spacing:1.2px;color:rgba(255,255,255,0.55)">BATTERIJEN</span>
                    <span style="font-size:11.5px;color:rgba(255,255,255,0.4)">onder ${d}% of stil</span>
                    <span style="font-size:11.5px;font-weight:600;padding:3px 11px;border-radius:20px;color:${kleur};background:rgba(255,255,255,0.06)">${n}</span></span>`;
                ]]]
              lijst: |
                [[[
                  var drempel = variables.drempel;
                  var reg = (hass && hass.entities) ? hass.entities : null;
                  var apparaten = (hass && hass.devices) ? hass.devices : null;
                  var gebieden = (hass && hass.areas) ? hass.areas : null;
                  var negeerInt = variables.negeer_integraties || [];
                  var negeerNaam = variables.negeer_namen ? new RegExp(variables.negeer_namen, 'i') : null;
                  // Ook hier per apparaat: een apparaat kan zowel een
                  // percentagesensor als een 'bijna leeg'-binarysensor hebben.
                  var groepen = {};
                  Object.keys(states).forEach(function(id){
                    var s = states[id];
                    if(!s || !s.attributes || s.attributes.device_class !== 'battery') return;
                    // Zelfde reden als bij de teller hierboven: instellingen zijn
                    // geen metingen.
                    if(id.lastIndexOf('sensor.', 0) !== 0 && id.lastIndexOf('binary_sensor.', 0) !== 0) return;
                    if(negeerNaam && negeerNaam.test(s.attributes.friendly_name || id)) return;
                    var niveau = null, binair = false, stil = false;
                    if(s.state === 'unavailable'){
                      stil = true;
                    } else if(id.lastIndexOf('binary_sensor.', 0) === 0){
                      if(s.state !== 'on') return;
                      binair = true;
                    } else {
                      var v = Number(s.state);
                      if(isNaN(v) || v > drempel) return;
                      niveau = v;
                    }
                    var e = reg ? reg[id] : null;
                    if(e && (e.hidden || e.hidden_by)) return;
                    var integratie = (e && e.platform) ? e.platform : id.split('.')[0];
                    if(negeerInt.indexOf(integratie) !== -1) return;
                    var devId = (e && e.device_id) ? e.device_id : null;
                    var dev = (devId && apparaten) ? apparaten[devId] : null;
                    var sleutel = devId || ('los:' + id);
                    var gebiedId = (e && e.area_id) || (dev && dev.area_id) || null;
                    var gebied = (gebiedId && gebieden && gebieden[gebiedId]) ? gebieden[gebiedId].name : null;
                    var naam = dev ? (dev.name_by_user || dev.name || devId)
                      : ((s.attributes.friendly_name || id).replace(/ ?batter[iy][j]?[ -]?(level|niveau|low|laag)?/i, '').trim() || id);
                    if(!groepen[integratie]) groepen[integratie] = {};
                    var g = groepen[integratie];
                    if(!g[sleutel]) g[sleutel] = {naam: naam, gebied: gebied, niveau: niveau, binair: binair, stil: stil, dev_id: devId, ent: id};
                    else {
                      // percentage wint van een enkel 'laag'-signaal; anders de laagste
                      if(stil) g[sleutel].stil = true;
                      if(niveau !== null && (g[sleutel].niveau === null || niveau < g[sleutel].niveau)) {
                        g[sleutel].niveau = niveau; g[sleutel].binair = false;
                      }
                    }
                  });
                  var namen = Object.keys(groepen);
                  if(!namen.length) return `<span style="font-size:14px;color:#4ade80">Geen bijna lege of stille batterijen.</span>`;
                  // Stil bovenaan (-2), dan een kaal 'laag'-signaal (-1), dan op percentage.
                  var sleutelVan = function(x){ return x.stil ? -2 : (x.niveau === null ? -1 : x.niveau); };
                  var kleurVan = function(x){
                    if(x.stil || x.niveau === null) return '#ef4444';
                    return x.niveau <= 10 ? '#ef4444' : (x.niveau <= 20 ? '#f59e0b' : 'rgba(255,255,255,0.7)');
                  };
                  var lijstVan = {};
                  namen.forEach(function(g){
                    lijstVan[g] = Object.keys(groepen[g]).map(function(k){ return groepen[g][k]; })
                      .sort(function(a,b){ return sleutelVan(a) - sleutelVan(b); });
                  });
                  namen.sort(function(a,b){ return sleutelVan(lijstVan[a][0]) - sleutelVan(lijstVan[b][0]); });
                  // Rij klikbaar maken; zie de toelichting bij de Matter-kaart.
                  var klik = function(devId, entId, inner){
                    var doel = devId ? ('/config/devices/device/' + devId) : null;
                    var actie = doel
                      ? `history.pushState(null,'','${doel}');window.dispatchEvent(new CustomEvent('location-changed'));`
                      : `this.dispatchEvent(new CustomEvent('hass-more-info',{detail:{entityId:'${entId}'},bubbles:true,composed:true}));`;
                    return `<a href="${doel || ('/history?entity_id=' + entId)}"
                      style="display:block;color:inherit;text-decoration:none;cursor:pointer;pointer-events:auto"
                      onpointerdown="event.stopPropagation()"
                      onpointerup="event.stopPropagation();${actie}"
                      onclick="event.preventDefault();event.stopPropagation()">${inner}</a>`;
                  };
                  var blokken = namen.map(function(g){
                    var rijen = lijstVan[g].map(function(x){
                      var plek = x.gebied ? `<span style="color:rgba(255,255,255,0.35)"> · ${x.gebied}</span>` : '';
                      var pct = x.niveau === null ? 100 : Math.max(2, x.niveau);
                      var waarde = x.stil ? 'stil' : (x.niveau === null ? 'laag' : x.niveau + '%');
                      return klik(x.dev_id, x.ent, `<div style="display:flex;align-items:center;gap:10px;padding:5px 0;border-top:1px solid rgba(255,255,255,0.04)">
                        <span style="flex:1;text-align:left;font-size:13.5px;color:rgba(255,255,255,0.85);white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${x.naam}${plek}</span>
                        <span style="width:56px;height:6px;border-radius:3px;background:rgba(255,255,255,0.07);overflow:hidden;flex:0 0 auto">
                          <span style="display:block;width:${pct}%;height:100%;background:${kleurVan(x)}"></span></span>
                        <span style="width:44px;text-align:right;font-size:13px;font-weight:600;color:${kleurVan(x)}">${waarde}</span></div>`);
                    }).join('');
                    return `<div style="margin-bottom:12px">
                      <div style="display:flex;align-items:baseline;gap:8px;padding-bottom:2px;text-align:left">
                        <span style="font-size:12.5px;font-weight:700;color:#38bdf8">${g}</span>
                        <span style="font-size:11.5px;color:rgba(255,255,255,0.4)">${lijstVan[g].length}</span></div>
                      ${rijen}</div>`;
                  }).join('');
                  return `<div style="max-height:calc(100vh - 190px);overflow-y:auto">${blokken}</div>`;
                ]]]

          # =============== MATTER APPARATEN ===============
          # Matter kent in de frontend geen 'laatst gezien'-veld; het dichtste
          # dat we hebben is last_updated van de entiteiten (elke rapportage van
          # het apparaat schrijft die bij). Per apparaat telt de nieuwste, dus
          # bovenaan staat het apparaat dat het langst niets heeft gestuurd.
          # Let op: een sensor die alleen bij verandering meldt (deurcontact)
          # kan legitiem uren stil zijn — oud betekent hier 'niets gehoord',
          # niet per se 'stuk'. Onbereikbare apparaten krijgen wel een label.
          # Apparaten die sinds de laatste herstart niets hebben gestuurd zeggen
          # 'sinds herstart' in plaats van een leeftijd die toevallig gelijk is
          # aan de uptime: die twee zijn niet hetzelfde en zagen er wel zo uit.
          - type: custom:button-card
            entity: sensor.time
            triggers_update: sensor.time
            show_icon: false
            show_name: false
            show_state: false
            tap_action:
              action: none
            variables:
              # Vanaf hier vindt de kaart het de moeite waard om te kleuren
              oud_uren: 6
              stil_uren: 24
              herstart_marge_min: 3
            styles:
              card:
                - background: var(--ov-panel-bg)
                - border: var(--ov-panel-border)
                - border-radius: 20px
                - box-shadow: none
                - padding: 16px 18px
              grid:
                - grid-template-areas: '"t" "lijst"'
                - row-gap: 12px
              custom_fields:
                t:
                  - justify-self: stretch
                lijst:
                  - justify-self: stretch
            custom_fields:
              t: |
                [[[
                  var reg = (hass && hass.entities) ? hass.entities : null;
                  var devs = {};
                  if(reg) Object.keys(states).forEach(function(id){
                    var e = reg[id];
                    if(!e || e.platform !== 'matter') return;
                    if(e.hidden || e.hidden_by) return;
                    devs[e.device_id || ('los:' + id)] = true;
                  });
                  var n = Object.keys(devs).length;
                  return `<span style="display:flex;align-items:baseline;gap:12px;width:100%">
                    <span style="font-size:12px;font-weight:700;letter-spacing:1.2px;color:rgba(255,255,255,0.55)">MATTER</span>
                    <span style="font-size:11.5px;font-weight:600;padding:3px 11px;border-radius:20px;color:rgba(255,255,255,0.7);background:rgba(255,255,255,0.06)">${n}</span>
                    <span style="margin-left:auto;font-size:11.5px;color:rgba(255,255,255,0.4)">oudste eerst</span></span>`;
                ]]]
              lijst: |
                [[[
                  var nu = Date.now();
                  var reg = (hass && hass.entities) ? hass.entities : null;
                  var apparaten = (hass && hass.devices) ? hass.devices : null;
                  var gebieden = (hass && hass.areas) ? hass.areas : null;
                  if(!reg) return `<span style="font-size:12.5px;color:#f59e0b">Entiteitenregister niet beschikbaar — Matter-apparaten zijn niet te herkennen.</span>`;
                  // Na een herstart staat elke last_updated op het herstartmoment,
                  // dus zonder deze correctie lijkt ieder stil apparaat precies zo
                  // lang stil als HA draait. Zie de kaart hierboven.
                  var startS = states['sensor.home_assistant_gestart'];
                  var startMs = startS ? new Date(startS.state).getTime() : NaN;
                  var uptime = isNaN(startMs) ? null : nu - startMs;
                  var marge = (variables.herstart_marge_min || 3) * 60000;
                  var groepen = {};
                  Object.keys(states).forEach(function(id){
                    var s = states[id];
                    var e = reg[id];
                    if(!s || !e || e.platform !== 'matter') return;
                    if(e.hidden || e.hidden_by) return;
                    var devId = e.device_id || null;
                    var dev = (devId && apparaten) ? apparaten[devId] : null;
                    var sleutel = devId || ('los:' + id);
                    var gebiedId = e.area_id || (dev && dev.area_id) || null;
                    var gebied = (gebiedId && gebieden && gebieden[gebiedId]) ? gebieden[gebiedId].name : null;
                    var naam = dev ? (dev.name_by_user || dev.name || devId)
                                   : ((s.attributes && s.attributes.friendly_name) || id);
                    var tijd = s.last_updated ? new Date(s.last_updated).getTime() : null;
                    if(!groepen[sleutel]) groepen[sleutel] = {naam: naam, gebied: gebied, aantal: 0, laatst: null, weg: 0, dev_id: devId, ent: id};
                    var g = groepen[sleutel];
                    g.aantal++;
                    if(s.state === 'unavailable' || s.state === 'unknown') g.weg++;
                    // nieuwste rapportage van dit apparaat telt
                    if(tijd !== null && (g.laatst === null || tijd > g.laatst)) g.laatst = tijd;
                  });
                  var lijst = Object.keys(groepen).map(function(k){ return groepen[k]; });
                  if(!lijst.length) return `<span style="font-size:14px;color:rgba(255,255,255,0.6)">Geen Matter-apparaten gevonden.</span>`;
                  // Oudste eerst; apparaten zonder tijdstempel helemaal bovenaan
                  lijst.sort(function(a,b){
                    if(a.laatst === b.laatst) return a.naam.localeCompare(b.naam);
                    if(a.laatst === null) return -1;
                    if(b.laatst === null) return 1;
                    return a.laatst - b.laatst;
                  });
                  var duurTxt = function(ms){
                    if(ms === null) return 'onbekend';
                    if(uptime !== null && Math.abs(ms - uptime) < marge) return 'sinds herstart';
                    var m = Math.floor(ms / 60000);
                    if(m < 1) return 'net';
                    if(m < 60) return m + ' min';
                    var u = Math.floor(m / 60);
                    if(u < 48) return u + ' uur';
                    var d = Math.floor(u / 24);
                    return d + ' ' + (d === 1 ? 'dag' : 'dagen');
                  };
                  var oud = (variables.oud_uren || 6) * 3600000;
                  var stil = (variables.stil_uren || 24) * 3600000;
                  var kleurVan = function(ms){
                    if(ms === null) return '#ef4444';
                    if(ms > stil) return '#ef4444';
                    if(ms > oud) return '#f59e0b';
                    return 'rgba(255,255,255,0.6)';
                  };
                  // Rij klikbaar maken. Drie dingen zitten een gewone link hier
                  // in de weg, dus alle drie afgevangen:
                  //  - button-card vangt de tik zelf af op de kaart, daarom
                  //    stopPropagation op pointerdown/-up en navigeren we op
                  //    pointerup in plaats van op click;
                  //  - een ouder-element met pointer-events:none, daarom
                  //    expliciet pointer-events:auto op de link zelf;
                  //  - een <a href> zou de hele frontend herladen, daarom
                  //    history.pushState + 'location-changed' (de HA-router
                  //    luistert daarop) en een preventDefault op de click.
                  // Draait inline JS niet, dan doet die preventDefault het ook
                  // niet en navigeert de href alsnog — trager, maar het werkt.
                  // Losse entiteiten hebben geen apparaatpagina; die openen hun
                  // more-info-dialoog via een event dat uit de kaart omhoog bubbelt.
                  var klik = function(devId, entId, inner){
                    var doel = devId ? ('/config/devices/device/' + devId) : null;
                    var actie = doel
                      ? `history.pushState(null,'','${doel}');window.dispatchEvent(new CustomEvent('location-changed'));`
                      : `this.dispatchEvent(new CustomEvent('hass-more-info',{detail:{entityId:'${entId}'},bubbles:true,composed:true}));`;
                    return `<a href="${doel || ('/history?entity_id=' + entId)}"
                      style="display:block;color:inherit;text-decoration:none;cursor:pointer;pointer-events:auto"
                      onpointerdown="event.stopPropagation()"
                      onpointerup="event.stopPropagation();${actie}"
                      onclick="event.preventDefault();event.stopPropagation()">${inner}</a>`;
                  };
                  var rijen = lijst.map(function(x){
                    var sinds = x.laatst === null ? null : nu - x.laatst;
                    var plek = x.gebied ? `<span style="color:rgba(255,255,255,0.35)"> · ${x.gebied}</span>` : '';
                    // Alleen melden als het hele apparaat weg is; losse entiteiten
                    // die op unavailable staan zeggen te weinig om te tonen.
                    var status = x.weg === x.aantal
                      ? `<span style="font-size:11.5px;font-weight:600;color:#ef4444;white-space:nowrap">niet bereikbaar</span>`
                      : `<span style="font-size:11.5px;color:rgba(255,255,255,0.45);white-space:nowrap">${x.aantal} ${x.aantal === 1 ? 'entiteit' : 'entiteiten'}</span>`;
                    return klik(x.dev_id, x.ent, `<div style="display:flex;align-items:baseline;gap:10px;padding:5px 0;border-top:1px solid rgba(255,255,255,0.04)">
                      <span style="flex:1;text-align:left;font-size:13.5px;color:rgba(255,255,255,0.85);white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${x.naam}${plek}</span>
                      ${status}
                      <span style="font-size:13px;font-weight:600;white-space:nowrap;color:${kleurVan(sinds)}">${duurTxt(sinds)}</span></div>`);
                  }).join('');
                  return `<div style="max-height:calc(100vh - 190px);overflow-y:auto">${rijen}</div>`;
                ]]]

      # =============== WAAKHOND ===============
      # De kaarten hierboven tellen álle entiteiten en slaan pas aan bij een
      # hoop tegelijk. Deze twee sensoren kijken juist naar een korte lijst met
      # naam en toenaam - één weggevallen lux-meter valt daarboven weg, maar
      # maakt de zonwering wel blind. Zie packages/9 - Other/Watchdog.yaml.
      - type: entities
        title: Waakhond
        state_color: true
        entities:
          - entity: sensor.watchdog_kritieke_sensoren
            name: Kritieke sensoren onbereikbaar
          - entity: sensor.watchdog_kritieke_sensoren
            type: attribute
            attribute: namen
            name: Welke
          - type: divider
          - entity: sensor.watchdog_backup_leeftijd
            name: Laatste backup (dagen geleden)
          - entity: sensor.watchdog_backup_leeftijd
            type: attribute
            attribute: laatste_backup
            name: Backup gemaakt op
